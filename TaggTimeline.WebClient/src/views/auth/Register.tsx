import { FunctionComponent } from "react";
import RegisterForm from "../../components/forms/RegisterForm";

/**
 * Page for logging in
 */
export const AuthRegisterPage: FunctionComponent = () => {
  return <RegisterForm />;
};

export default AuthRegisterPage;
